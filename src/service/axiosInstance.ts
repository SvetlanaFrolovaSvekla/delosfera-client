/**
 * Единый HTTP-клиент проекта.
 *
 * Раньше существовали два независимых axios-клиента (`axiosInstance` и `apiClient`)
 * с продублированной логикой авторизации и обновления токена, к тому же с разными
 * заголовками языка и лишним console.log в каждом запросе. Оставлен один клиент
 * (`apiClient` — с обновлением токена через navigator.locks, безопасным между
 * вкладками). Этот модуль ре-экспортирует его как `axiosInstance` для обратной
 * совместимости со старыми импортами.
 */
export {apiClient as axiosInstance} from "@/service/apiClient.ts";
